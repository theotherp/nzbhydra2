# FM-191: One List-Editor Transaction Hook

Status: planned Owner:
Feature IDs: F-CONFIG-AUTH, F-CONFIG-CATEGORIES, F-CONFIG-DOWNLOADING, F-CONFIG-EXTERNAL-TOOLS, F-CONFIG-INDEXERS, F-CONFIG-SEARCHING
Component IDs: C-CONFIG-FORM
API IDs: None
Depends on: None
Blocks: None

## Outcome

Five config list editors carry the same hand-copied modal-transaction machinery FM-064 established: an
`Editing = {index, token, value}` state, a `transactionRef` counter bumped on open, close and delete, and a commit that
drops a resolved transaction whose token is stale or whose index no longer exists. `CustomMappingsSection` has the modal
shape but no `token` field at all (`:39-43`, `:87-102`), so it is the one editor where a stale commit has no guard —
today only because its dialog happens to be synchronous. One `useListEditorTransaction` hook in `features/config/` owns
the lifecycle and the staleness rules for all six; each section keeps its own dialog, array write and confirm step.
Behaviour-preserving. Backlog item 22; one packet because the hook's interface is only readable off all six call sites.

## Decision Dependencies

ADR-0033 (bespoke per-list markup stands; this unifies the transaction, not the tables), ADR-0034 (the categories
placeholder-add and its unmount rollback are load-bearing), ADR-0002, ADR-0014.

## Files Allowed To Modify

- New `core/ui-react/src/features/config/useListEditorTransaction.ts` and its test file
- core/ui-react/src/features/config/{auth/AuthUsersSection,categories/CategoriesTable,downloading/DownloadersSection,external-tools/ExternalToolsSection,indexers/IndexersConfigTab,searching/CustomMappingsSection}.tsx
- Their suites, import and helper edits only: `auth/AuthUsersSection.test.tsx`, plus `{categories/CategoriesConfigTab,downloading/DownloadingConfigTab,external-tools/ExternalToolsConfigTab,indexers/IndexersConfigTab,searching/SearchingConfigTab}.test.tsx`
- docs/frontend-migration/COMPONENTS.yaml (C-CONFIG-FORM note), STATUS.md, this packet

## Out Of Scope

- `notifications/NotificationEntriesSection.tsx`: it edits entries in place with no draft, dialog or async gap — its
  `write`/`currentEntries` pair is not a transaction, and giving it one is a behaviour change. Declare it inspected.
- The six dialogs and their async checks; `IndexerTable`, `DownloaderTable`, `RepeatSection`; the tables' markup
- `IndexersConfigTab`'s bulk `capsCheckRef` recheck may consume the hook only at no behaviour cost; bespoke is fine
- Any change to which index an entry binds to, to commit merge semantics, or to the config write payload

## Context To Read

- The five implementations: `AuthUsersSection.tsx:49-199`, `CategoriesTable.tsx:51-301` (placeholder add `:199-209`,
  unmount rollback `:168-185`, cancel `:230-240`), `DownloadersSection.tsx:20-123`, `ExternalToolsSection.tsx:47-146`,
  `IndexersConfigTab.tsx:94-259` (row token `:174`, bulk token `:179`); and `CustomMappingsSection.tsx:39-149`, the
  unguarded editor this task brings into the pattern
- The two divergences the hook must not erase: commit merges by spread everywhere except `IndexersConfigTab.tsx:233-239`,
  which replaces wholesale on purpose; and only `AuthUsersSection`/`CategoriesTable` guard `index >= current.length`
- `STATUS.md` FM-064 (why the token exists: a resolved check for a superseded transaction) and FM-119's correction cycle;
  `C-CONFIG-FORM` in `COMPONENTS.yaml`, and `C-CONFIG-FIELDS`'s note that a modal-transaction list is not `RepeatSection`

## Acceptance

- `useListEditorTransaction` owns the `{index, token, value}` state, the monotonic token counter, `open`, `close`,
  `cancel`, an `isCurrent(token)` predicate, and a guarded commit that runs the caller's write only when the token is
  current and the index is still in range. The array write, merge vs replace, confirm dialogs, the placeholder add and
  its unmount rollback, and per-section draft fields stay in the call site.
- All six sections use it and hold no `transactionRef` of their own. `CustomMappingsSection` gains the guard by adoption:
  reach for a red-first case in `SearchingConfigTab.test.tsx` through the section's own API; if that state is unreachable
  while `CustomMappingDialog` stays synchronous, say so and let the hook's unit test carry the proof — never make it
  reachable by changing the dialog.
- Every existing case in the six suites keeps its assertions — the FM-064 stale-check cases
  (`DownloadingConfigTab.test.tsx` from `:467`, `ExternalToolsConfigTab.test.tsx` from `:837`) and FM-119's abandoned-add
  and blank-name cases included. Only imports, helper names and the new case may change; a changed assertion is a
  required finding. Every `data-testid` survives unchanged, and entries stay bound to the config array index.
- The hook's own unit test covers: a stale-token commit is dropped; a commit whose index no longer exists is dropped; a
  delete makes an open transaction on a later row stale; a fresh transaction after a close commits.
- No screenshot strip: no rendering changes. Instead, for one section per merge strategy (`AuthUsersSection` and
  `IndexersConfigTab`), capture the section's `outerHTML` at base and after and show the pair byte-identical.

## Verification

- core/ui-react: `npm run test -- --run`, `typecheck`, `lint`, `format:check`, `build`, `knip`,
  `validate:focus-affordances` (red at base on five known false positives — compare, do not fix), `validate:migration`
- Repository root: `python3 misc/run_gui_systemtest.py --runtime local --skip-install --keep-services -- tests/config-auth.spec.ts tests/config-categories.spec.ts tests/config-downloading.spec.ts tests/config-indexers.spec.ts tests/config-searching.spec.ts tests/external-tools.spec.ts` (real backend) — passes, specs unedited
- `git diff --stat` shows no edit outside `Files Allowed To Modify`; no stray generated files

## Handoff / Review

Implementer fills `../templates/handoff.md` and marks the task `review`; a fresh reviewer fills `../templates/review.md`; only
the coordinator marks `done`.

## Agent Routing

Suggestions only; the coordinator may override and records why. Not part of the contract — the sections above govern.

- Implementer: `opus` — a new shared hook over six divergent call sites, guarding a concurrency rule whose failure modes
  (FM-064, FM-119) were found by review rather than by tests.
- Reviewer: `opus` — a new shared abstraction, so at least the implementer's tier; the staleness rules need re-derivation.
- Fixer: `opus` — findings here are concurrency-semantic, not mechanical.

Implementer prompt: Read all six sections before writing the hook; fix its signature against the two hardest call sites
(`CategoriesTable`, `IndexersConfigTab`) first. Trap: a hook that also owns the array write cannot serve both merge
strategies — leave the write with the caller. Trap: the categories placeholder sits in the real array, not in the draft.
Reviewer prompt: Check hardest that no section's commit semantics changed (merge vs replace, index-range guard) and that
the categories unmount rollback still keys on the same token. Distrust a blanket "no behaviour change" claim: for
`CustomMappingsSection` it deliberately does.
