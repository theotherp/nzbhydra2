# FM-190: One History Page Hook And Frame

Status: ready Owner:
Feature IDs: F-HISTORY-SEARCHES, F-HISTORY-DOWNLOADS, F-HISTORY-NOTIFICATIONS
Component IDs: C-HISTORY-PAGER, C-HISTORY-REQUEST, C-HISTORY-REFINE-BAR
API IDs: None
Depends on: None
Blocks: None

## Outcome

`SearchHistoryPage.tsx` (1025), `DownloadHistoryPage.tsx` (540) and `NotificationHistoryPage.tsx` (423) each restate the
same page plumbing: the `useHistoryFilterCriteria` destructure, the page/size/sort read out of `historySearchParams`, the
`useQuery` whose key is the derived filter *model* with `placeholderData: keepPreviousData` and a threaded `signal`, the
`updateSort` and `changePageSize` handlers, and the header / refreshing-status / malformed-count / empty-state / scroller /
pager frame around the table. FM-110 unified only the four provably identical shapes; this task takes the remaining
plumbing — a `useHistoryPage` hook plus a frame component in `features/stats/history/` — leaving each page as its
dimensions, its columns and its rows. Behaviour-preserving: no rendered output, `data-testid`, URL shape or request
changes. Backlog item 21; the three routes move together because the hook's shape is only readable off all three.

## Decision Dependencies

ADR-0014 (no per-feature style modules; the frame states no new tokens), ADR-0046 (the refine surface stays the chrome).

## Files Allowed To Modify

- core/ui-react/src/features/stats/history/{Search,Download,Notification}HistoryPage.tsx and their `.test.tsx`
- New `core/ui-react/src/features/stats/history/useHistoryPage.ts` and `HistoryPageFrame.tsx` plus one test file each
- docs/frontend-migration/COMPONENTS.yaml (C-HISTORY-PAGER / C-HISTORY-REQUEST notes only), STATUS.md, this packet

## Out Of Scope

- `SavedSearchesPage.tsx`; `HistoryPager.tsx`; `refine/`; `historySearchParams.ts`; `useHistoryFilterCriteria.ts`;
  `features/stats/shared/`; anything under `api/` — including the `showsUsername`/`showsIp` duplication
  (`api/searchHistory.ts:96-97`, `api/history/downloads.ts:81-82`), a `MAINTENANCE.md` single-session-fix candidate
- `SearchHistoryPage`'s local `SortHeader` (`:638-662`): it renders a `Button`, not `../shared/SortHeader`'s
  `TableSortLabel`, so folding it in changes anatomy — FM-110 excluded it for that reason and so does this task
- `SearchHistoryPage`'s details dialog and its second `useQuery`, its `showUserAgent` latch (`:106-135`, item 13b,
  `ff8ddcf64`) and its repeat action; `DownloadHistoryPage`'s status/title cells; `NotificationHistoryPage`'s `SafeText`

## Context To Read

- The three pages' bodies: `SearchHistoryPage.tsx:80-235`, `DownloadHistoryPage.tsx:65-179`,
  `NotificationHistoryPage.tsx:55-161` — the duplicated region, including the verbatim-copied comments on the filter-model
  query key and on `keepPreviousData`, and `updateSort` (`:194-202` / `:139-147` / `:118-126`)
- The abort-signal threading from `ce4fbfdad` (backlog item 19) in each `queryFn`, which the hook must carry unchanged
- `STATUS.md` FM-110 (what was already unified and the exclusion that held) and FM-136/FM-111 for the byte-identity method
  this task reuses; `MAINTENANCE.md`'s `showsUsername`/`showsIp` and local-`SortHeader` entries; `C-HISTORY-PAGER`,
  `C-HISTORY-REQUEST` and `C-HISTORY-REFINE-BAR` in `COMPONENTS.yaml`

## Acceptance

- `useHistoryPage` owns, for all three routes: the `useHistoryFilterCriteria` destructure, `page`/`pageSize`/`sort` from
  `historySearchParams`, the `useQuery` (same key tuple, same `placeholderData`, same threaded `signal`), and `updateSort`
  / `changePageSize`. Each page supplies its dimensions, sort columns, default sort, query-key prefix and fetcher; each
  page's `queryKey` and request body stay byte-identical to base (asserted from the page tests, not from the hook's).
- `HistoryPageFrame` owns the header row with the `<page>-history-refresh` button, the constant-height `role="status"`
  refreshing region, the malformed-count `Alert`, the empty-state `Alert` whose "Clear filters" action appears only when
  the active filter count is non-zero, the `TableScrollAffordance` and the `HistoryPager`. Heading text, loading and error
  messages, entry noun, `minWidth` (760 / 640 / 800) and every test id arrive as props.
- Every `data-testid` listed for the three routes in `FEATURES.yaml` survives with the same element and the same DOM
  position, `history-refine-*`, `history-pager`, `history-page-size` and the three `*-page-status` ids included.
- Rendered output is proven unchanged, not argued: for each of the three routes capture the page container's `outerHTML`
  and the emotion stylesheet at base and after, for the loaded, refreshing, empty-with-filters and error states, and show
  each pair byte-identical including class hashes (FM-136's method). Record the pairs' hashes in the handoff.
- The three page test files keep every case and assertion; edits are limited to imports and moved helpers' paths. A
  changed assertion is a required finding. No screenshot strip: the byte-identical captures are the stronger evidence.

## Verification

- core/ui-react: `npm run test -- --run`, `typecheck`, `lint`, `format:check`, `build`, `knip`,
  `validate:focus-affordances` (red at base on five known false positives — compare, do not fix), `validate:migration`
- Repository root: `python3 misc/run_gui_systemtest.py --runtime local --skip-install --keep-services -- tests/search-history.spec.ts tests/notification-history.spec.ts tests/downloads.spec.ts` (real backend) — passes, specs unedited
- `git diff --stat` shows no edit to any file outside `Files Allowed To Modify`; no stray generated files

## Handoff / Review

Implementer fills `../templates/handoff.md` and marks the task `review`; a fresh reviewer fills `../templates/review.md`; only
the coordinator marks `done`.

## Agent Routing

Suggestions only; the coordinator may override and records why. Not part of the contract — the sections above govern.

- Implementer: `opus` — a new shared hook and frame whose interface must be read off three divergent call sites at once,
  with hook order and query identity to preserve across all of them.
- Reviewer: `opus` — new shared modules, so at least the implementer's tier; the identity claims must be re-derived, not read.
- Fixer: `opus` — a finding here is a behaviour or identity difference, not a wiring slip.

Implementer prompt: Start with the three page bodies side by side and write the hook's signature before moving any code.
Prove first, with a base-vs-after `outerHTML` capture on one route, that the frame emits identical markup — before doing
the other two. Trap: the query key contains the derived filter model, so a changed memo boundary silently changes refetch
behaviour. Trap: `SearchHistoryPage` has a second query and local state; do not pull either into the hook.
Reviewer prompt: Re-derive the byte identity yourself from `git show <base>:<path>`, and check hardest that no page test
assertion changed and that the abort signal still reaches every fetcher. Distrust any identity claim shown only as a diff.
