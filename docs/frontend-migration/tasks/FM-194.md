# FM-194: Custom Mapping Model, Ordering Semantics, And Batch Chain Test

Status: planned Owner:
Feature IDs: F-CONFIG-SEARCHING
Component IDs: None
API IDs: API-CONFIG-CUSTOM-MAPPING-TEST
Depends on: None
Blocks: FM-195

## Outcome

Custom query/title mappings become nameable, individually disableable, carry saved example inputs, and evaluate in a
deterministic order: top to bottom, stopping after the first whole-string (`matchAll`) mapping applied, replacing today's
"two whole-string mappings match -> log an error and apply none". The same change stops a `TITLE` mapping reaching result
titles and a `RESULT_TITLE` mapping reaching requests (which the stop rule would otherwise let cross-contaminate), and turns
`POST /internalapi/customMapping/test` into a batch at the same URL: many example lines against the whole edited list,
answering for the edited mapping and for the chain. Backend only, at the owner's 2026-09-13 request to split issue #902 by
layer against `tasks/README.md`'s "never split by layer" rule.

## Decision Dependencies

None. ADR-0018 (`FAIL_ON_NULL_FOR_PRIMITIVES` is off) constrains the request shape; owner decisions are in Acceptance.

## Files Allowed To Modify

- shared/mapping/src/main/java/org/nzbhydra/config/searching/CustomQueryAndTitleMapping.java
- core/src/main/java/org/nzbhydra/searching/CustomQueryAndTitleMappingHandler.java
- core/src/main/java/org/nzbhydra/config/validation/SearchingConfigValidator.java
- core/src/test/java/org/nzbhydra/searching/{CustomQueryAndTitleMappingHandlerTest,CustomQueryAndTitleCustomQueryAndTitleMappingHandlerTest}.java,
  and new core/src/test/java/org/nzbhydra/config/validation/SearchingConfigValidatorTest.java
- core/openapi.json plus core/ui-react/src/api/generated/openapi.ts regenerated from it (no hand edit);
  docs/frontend-migration/APIS.yaml (`API-CONFIG-CUSTOM-MAPPING-TEST` note), STATUS.md, this packet

## Out Of Scope

- `core/src/main/resources/changelog.yaml`: the owner holds an uncommitted edit there, so the handoff instead records, ready
  to paste, one `type: note` for the ordering change and one `type: fix` for the TITLE/RESULT_TITLE cross-application.
- Any config migration or `configVersion` bump (owner decision: field initializers suffice), `baseConfig.yml`, the
  `CustomQueryAndTitleMapping(String configValue)` constructor, and every `core/ui-react/src` file but `openapi.ts`.

## Context To Read

- `CustomQueryAndTitleMappingHandler.java`: `mapMetaData(MetaData, List)`'s filter chain and its two stop branches,
  `testMapping`, `isDatasetMatch`, `mapMetaData(MetaData, mapping)`, `TestRequest`/`TestResponse`; and the model itself
- `SearchingConfigValidator.java:50-66`, already collecting `warnings` beside `errors`; `APIS.yaml`
  `API-CONFIG-CUSTOM-MAPPING-TEST` (`isMatch` serializes as `match`; error wins over match); `STATUS.md` FM-069

## Acceptance

- `CustomQueryAndTitleMapping` gains `String name` (null), `boolean enabled = true`, `List<String> examples = new
  ArrayList<>()`, with no migration step and no `configVersion` bump: a deserialization test over content lacking all three
  keys proves an older config loads enabled and example-less.
- Relevant-mapping filtering separates the directions: for `RESULT_TITLE` metadata only `affectedValue == RESULT_TITLE`
  mappings are relevant; for a search request only `affectedValue != RESULT_TITLE` ones whose `searchType` matches; a
  mapping with `enabled == false` never is. The list then applies in order and stops after the first applied `matchAll`
  mapping, logged at debug under `LoggingMarkers.CUSTOM_MAPPING`; the multiple-matchAll error branch is gone.
- Handler tests, each red before the change: first of two matching whole-string mappings wins; a partial before a
  whole-string one is applied too; a partial after an applied whole-string one is not; a disabled mapping is skipped; a
  `TITLE` mapping does not reach a result title, nor a `RESULT_TITLE` mapping a request.
- The endpoint takes `{mappings, mappingIndex, examples}` and answers `{results: [{input, thisMapping: {match, output,
  error}, chain: {output, appliedIndices, error}}]}`. `mappingIndex` is a boxed `Integer`: absent or negative means chain
  only — a primitive `int` would default to 0 under ADR-0018 and silently test the first mapping. The chain forces
  `affectedValue = QUERY`, season 1, episode 2 as the single-mapping test does today, ignoring each mapping's own
  `affectedValue`; over 200 example lines is answered HTTP 400 unevaluated. An endpoint test asserts, for several examples
  through a two-mapping chain, each line's `thisMapping` match and output, chain output and `appliedIndices` (into the
  request's `mappings`, not the filtered list); plus the cap and a broken `from` reaching `error`, not HTTP 500.
- `SearchingConfigValidator` keeps the regex check over every mapping, disabled included, and adds a *warning* (not an
  error) naming both when two enabled `matchAll` mappings share an identical `from`; both tested.

## Verification

- Root: `mvn --batch-mode -o -pl org.nzbhydra:mapping install -DskipTests -q` (must precede any core test, or core compiles
  against the stale DTO), then `mvn -o -pl core test -DskipTests=false` (without the flag it skips) — green, "Tests run" > 0
- openapi: start the core (`--datafolder <tmp> --nobrowser`, port 5076), then `mvn -o -pl core
  org.springdoc:springdoc-openapi-maven-plugin:1.5:generate -DapiDocsUrl=http://127.0.0.1:5076/v3/api-docs`
- core/ui-react: `npm run generate:api`, `check:api`, `typecheck`, `build`, `validate:migration` — green (no red gate
  inherited). No screenshot strip: nothing renders.
- `git diff --stat` shows no edit outside `Files Allowed To Modify` (changelog.yaml included); no stray generated files

## Handoff / Review

Implementer fills `../templates/handoff.md` and marks the task `review`; a fresh reviewer fills `../templates/review.md`; only
the coordinator marks `done`.

## Agent Routing

Suggestions only; the coordinator may override and records why. Not part of the contract — the sections above govern.

- Implementer: `opus` — a persisted-model change, an evaluation-semantics change with a live bug fix inside it, and a
  replaced request/response contract, reconstructed from a handler whose filter chain hides the defect.
- Reviewer: `opus` — raised above the owner's sonnet default: an API contract and a persisted model change, where the role
  rule puts the reviewer at or above the implementer. Fixer: `opus`; findings here are semantic, not mechanical.

Implementer prompt: Start at `mapMetaData(MetaData, List)`'s filter chain and write the direction separation before the stop
rule — the stop rule makes the existing cross-application actively harmful, not merely wrong. Trap: entries in
`relevantMappings` have already matched, so "applied a matchAll mapping" needs no second match test. Prove first, red, that
a `TITLE` mapping no longer touches a result title.
Reviewer prompt: Check hardest that each handler case fails against the pre-change handler and that `appliedIndices` index
the request's own array. Distrust a red-first claim with no restored-source run behind it.
