# Migration Status

## Active

None.

## Review

None.

**FM-051 (`tasks/FM-051-consecutive-search-query-text.md`) is `done`** — reviewed **PASS WITH MINOR FINDINGS**, no required corrections. The reviewer diffed `canonicalSearch`'s emitted URL key list against baseline `b5b1b7b38` name-by-name and order-by-order to confirm the contract is byte-identical, traced all four deep-link cases through the code rather than trusting their names, reproduced the full SHA-256 manifest and every cheap deterministic command first-hand (247/247 across 38 files), and independently verified the fail-first near-miss mechanism in `core/src/main/java/org/nzbhydra/web/MainWeb.java`'s `isReactSelected` — confirming that a bare `page.goto("/")` really does land on the legacy shell, and that the committed test explicitly selects React instead. It confirmed the `8 → 10` lint-warning delta is exactly the two new exports tripping the same `react-refresh/only-export-components` rule that already fires on that file's pre-existing exports, not padding. **One minor finding is carried, not fixed:** the implementer's own run left git-ignored `tests/system/playwright-report/` and `test-results/` artifacts in the working tree, and the handoff does not record the packet's required confirmation that no stray generated files remain. They are git-ignored, so no tracked scope or diff is affected. It fixes a pre-existing,
user-observable defect FM-049's implementer discovered while building a two-search fixture and recorded in that packet's Follow-Up Work: submitting two distinct plain-text searches back-to-back in one `SearchWorkspace` session made the
**second** search silently resubmit the **first** query's text — to `/internalapi/search`, to the URL, and therefore to the results the user saw, while the search box still showed what they typed. `/fm-quickfix` correctly refused it on
two gate criteria (a behavioral change across two modules, and a change to a user-observable surface), and the obvious one-line inversion was wrong, so it was a packet.

**No ADR was required, and the designer did not make the call — the repository already had.** The URL was suspected of being a lossy representation, two form fields sharing one `query` parameter. It is not. The single visible
`search-query` input registers to `title` when the selected category's `searchType` maps to a media type and to `query` otherwise (`SearchWorkspace.tsx:361-417`), and `category` is already in the URL, so the discriminator round-trips
intact. What ignored it was the derivation: both `canonicalSearch()` and `SearchPage.submit()` picked the text with a `values.title || values.query` precedence chain, so after the first search's own `navigate()` round-trip — which
mirrors the bare `query` param into both fields and remounts the form on its `key={JSON.stringify(initialValues)}` — the invisible mirrored `title` outranked the freshly typed, visible `query`. Legacy settles it literally:
`core/ui-src/js/search-controller.js:418-419`'s `getSearchQuery()` returns the one box unconditionally when no media item is selected, with no fallback chain, and `:43`/`:50-52` are the read rule `valuesFromSearch` already mirrors
faithfully. **The fix is one exported `nonIdentifierQueryText(values, catalog)`** in `SearchWorkspace.tsx`, used by both `canonicalSearch()` and `SearchPage.submit()`, selecting the field the visible box is actually bound to via the
same `mediaTypeForCategory(...)` resolution the render already used — never a `title || query` fallback. `SearchPage.tsx`'s local `hasMediaIdentifiers` and `SearchWorkspace.tsx`'s `hasIdentifier` (byte-equivalent five-identifier-field
predicates) were collapsed into one exported `hasIdentifier`. `canonicalSearch` gained a `catalog` parameter — a signature change, not a URL change — and **adds no URL parameter and changes no emitted key set**; deep-link compatibility
is preserved by construction, proven for all four required cases including the media-category `?query=` deep link that pins `valuesFromSearch`'s mirror as load-bearing against a well-meaning cleanup. `categoryChanged`'s own second
manifestation (it clears `title` but never `query`, so switching from "All" to a media category and submitting an empty box could send a stale `query`) needed no separate code change: the shared derivation never reads `query` for a
media category, so the one-function fix already covers it, and a dedicated `SearchWorkspace.test.tsx` case proves it. `additionalQuery`'s pre-existing precedence in `submit()` (it wins over the shared derivation, while `canonicalSearch`
drops it from the URL for a no-identifier search) was deliberately left untouched per the packet's Out Of Scope, and is now pinned by a dedicated byte-identical-behavior test rather than merely left alone. `F-SEARCH-FORM` stays
`visual.status: proposed` with the acceptance outstanding since FM-044 neither withdrawn nor re-dated; nothing here is visual work, and `FEATURES.yaml` was confirmed unchanged rather than edited — its `tests` list already named all
four test files and this defect was never recorded as a `gaps` entry.

The regression proof was obtained at both required layers for two different reasons, and the new assertions were observed **failing** against the unfixed source before the fix, not merely asserted to have been. At the component
layer (`SearchPage.test.tsx`), the mocked router's recorded `navigate` argument was fed back into `router.search` and the component re-rendered to trigger the real `key={JSON.stringify(initialValues)}` remount; eight new assertions
across `SearchWorkspace.test.tsx` and `SearchPage.test.tsx` were confirmed to fail against the unfixed source (`TypeError: nonIdentifierQueryText is not a function` for the pure-derivation cases; wrong-value assertions for the
canonicalSearch-mirror, `categoryChanged`, and consecutive-search cases), and both touched source files were then restored and confirmed byte-identical to baseline `b5b1b7b38` by SHA-256 before the passing run counted. At the system
layer (`tests/system/tests/search.spec.ts`), the same fail-first discipline caught an implementer mistake before it reached the evidence: the new test's first draft relied on the shared `beforeEach`'s bare `page.goto("/")`, which
carries no `nzbhydra-ui=react` cookie and so serves the **legacy** AngularJS shell by default (`MainWeb.isReactSelected`) — legacy has no such defect, so that draft passed even against unfixed React source, for the wrong reason.
Corrected to explicitly select React first (`page.goto("ui/react?redirect=/")`, the same pattern the ADR-0012 test above and every `results.spec.ts` test already use), it then failed against the unfixed source with a genuine
30-second `page.waitForResponse` timeout waiting for the second search's own query text, and passed after the fix. The whole, un-narrowed `search.spec.ts` file passes against a real Maven-built JVM backend, mockserver, and the
sonarr/radarr Docker fixtures: **16 passed (30.5s)**, exit 0 — the 15 standing after FM-050 plus this packet's one new block. The stale in-repo comment at `tests/system/tests/search.spec.ts` (recorded under FM-049's Follow-Up Work,
formerly `:717-722`) was corrected to say the defect is fixed, keeping the `page.goto` FM-050's own recorded verification evidence depends on, with no assertion in that test touched.

One deviation is disclosed rather than hidden: `npm run lint` gained two new `react-refresh/only-export-components` warnings (0 errors either way; 8 -> 10 total) at the two new exports (`nonIdentifierQueryText`, `hasIdentifier`) this
task's own Acceptance mandates living in `SearchWorkspace.tsx` as "a module-local exported pure function" rather than a new shared file (`COMPONENTS.yaml` is explicitly Out Of Scope). This is the same pre-existing warning class the
file already carried for `valuesFromSearch`/`canonicalSearch`/`searchFormSchema`, not a new rule, and is the mechanical, unavoidable consequence of the packet's own design mandate rather than an oversight. Two existing
`SearchWorkspace.test.tsx` round-trip assertions passed a `category: "Cinema"`/`query` input the form cannot itself produce (a media category with text in `query` and an empty `title`); the input was changed to its reachable
equivalent (`title`/`query` swapped) with the assertion itself left unchanged, per the packet's explicit allowance for exactly this case.

**FM-049 is `done`** — reviewed **PASS** with no findings by a fresh independent reviewer, which audited the *measurement* rather than the conclusion, as the packet's `Fresh Review` section required: it confirmed the trace covers all nine probes, that the controls demonstrate detectability by observing `Enter`/`Space` issue real search requests rather than merely matching a locator, and that the accessibility evidence is a genuine CDP `Accessibility.getFullAXTree` capture rather than a Playwright role query. It reproduced both byte-identity claims by SHA-256, reran `tsc --noEmit`, `prettier --check .`, and `validate:migration` first-hand, reused the expensive real-backend Playwright run under its evidence-reuse exception, and independently checked the *Children Presentational* refutation against the WAI-ARIA 1.2 characteristics table — confirming the base `menuitem` role carries no such row, so Option A1's precondition genuinely holds. **ADR-0012 (`decisions/ADR-0012-recent-search-refill-keyboard-reachability.md`) is accepted — 2026-08-18, explicit decision of the repository owner: Option A1.** `ArrowRight` moves focus onto the existing nested
Refill `IconButton`, `ArrowLeft`/`Escape` returns focus to the row, `Enter`/`Space` activates it natively, and `aria-keyshortcuts` on the row announces it; the row keeps one `menuitem`, one MUI roving-focus stop, and its current
structure. Options B–F are recorded there as rejected with their tradeoffs intact, including the proposer's own contrary view that reverting to two rows is the cheapest genuinely correct answer, which was put to the owner in full
before they chose. Accepting the ADR is **not** visual acceptance and no agent may supply it.

FM-049 itself was a measurement packet and implemented no remedy by design, so its work is complete and nothing further belongs to it. It established, in a real Chromium browser against a real JVM backend, that the recent-search Refill
action is **unreachable by keyboard**: over a keyboard-opened menu with two deterministic entries, no tested key (`ArrowDown`/`ArrowUp`/`Home`/`End`/`Tab`/`Shift+Tab`/`Enter`/`Space`/type-ahead `r`) ever lands focus on it, with the
trace's controls confirming it would detect reachability where it exists. The accessibility-tree half of the disposition rule holds — Chromium's computed tree (CDP `Accessibility.getFullAXTree`) exposes the nested control as a
non-ignored, focusable `button` with the correct `Refill: <description>` name — so this is a pure focus-navigation gap, not a tree-pruning one. `RecentSearches.tsx` and `search.spec.ts` are byte-identical to baseline. It goes to
`review` rather than straight to `done` because it has never had an independent review, and its own *Fresh Review* section sets that reviewer an unusual obligation — audit the measurement, not the conclusion. Only the coordinator marks
it `done`.

The remedy is **FM-050** (`tasks/FM-050-recent-search-refill-keyboard-reachability-remedy.md`), created `planned` in this same refinement and depending on FM-049, which owns the `F-SEARCH-RECENT` registry edits it discharges. FM-050
carries every standing obligation ADR-0012's `Consequences` places on the remedy and does not itself settle: discoverability actively addressed (`aria-keyshortcuts="ArrowRight"` plus one shared visible hint node in the menu, with the
packet fixing the literal text and the implementer required to state and justify its realization); mandatory real-browser keyboard verification with a negative-control probe, since jsdom has no roving focus and this defect was only
ever detectable in a browser; a full specification of the new focus stop's otherwise-undefined states (`ArrowDown`/`ArrowUp`/`Home`/`End`/type-ahead from the button behave exactly as from the row, `Escape` returns focus without closing
the menu); and a re-verification duty, recorded in the code, that the `ArrowRight`/`ArrowLeft` binding is scoped to `@mui/material` `7.3.9` and must be re-proven after any MUI upgrade. It removes and renames no `data-testid`, is
additive across all three test layers, and leaves `F-SEARCH-RECENT` `visual.status: proposed` with the acceptance outstanding since FM-038 neither withdrawn nor re-dated — the hint is one additive state joining that same acceptance,
not a new one.

`F-SEARCH-RECENT`'s `backlog` was corrected in this refinement, by the task designer rather than by FM-049's implementer: it named a decision with no ID, five options, and FM-049 as the authority, all three of which the accepted ADR
made stale, and `README.md`'s *Registry Rules* require an unfinished record to name its next task or blocking ADR. It now names ADR-0012, its six options, the accepted option, and FM-050 as the next task. Only those stale facts
changed — no acceptance was created and nothing was re-dated — and FM-049's Handoff describes the text as it stood at handoff and was deliberately left as written.

**FM-050 (`tasks/FM-050-recent-search-refill-keyboard-reachability-remedy.md`) is `done`** — reviewed `FAIL` then `PASS` after one fixer cycle, by two fresh independent reviewers. The first review found two required defects, both in `tests/system/tests/search.spec.ts`: the packet's explicit requirement to assert the hint's horizontal padding against the row's *by browser computed style* was unmet, and the handoff had instead reasoned from MUI's source defaults — the exact substitution that criterion forbade — without disclosing it; and the focus-indicator assertion proved nothing, since `outlineOffset` computes to `3px` even under `outline-style: none`, while the handoff claimed it proved real keyboard-focus indication. Both were corrected additively, with no existing assertion weakened. The focus assertion now targets the real mechanism — `ButtonBase`'s pulsating `TouchRipple` — which the re-review verified against `@mui/material` 7.3.9 source would genuinely fail under `disableRipple` or `disableFocusRipple`, and verified is not flaky. **An app-wide finding is carried, not fixed here:** MUI `ButtonBase`'s unconditional `outline: 0` beats `theme.ts`'s `MuiCssBaseline` `:focus-visible` override for every `ButtonBase`-derived control in the app, so the only focus indication anywhere is that `0.3`-opacity ripple. Reproduced against an unrelated pre-existing nav link, so it is neither introduced by nor fixable within FM-050; it is recorded in the packet's Follow-Up Work as task-sized. It implements ADR-0012 Option A1: `ArrowRight` on a `recent-search-entry` row moves focus onto that row's existing Refill `IconButton`;
`ArrowLeft`/`Escape` on the button returns focus to the row without closing the menu, while `Escape` on a row still closes it, unchanged; `Enter`/`Space` activate the button natively with no handler added; `ArrowDown`/`ArrowUp`/`Home`/
`End` from the button are replayed on the row (via a refocus-then-redispatch of the same key) so `MenuList`'s own sibling-only `moveFocus` reaches the identical adjacent row it would from the row itself, rather than its nested-descendant
fallback; `Tab`/`Shift+Tab` were already position-independent (`Menu.js`'s `handleListKeyDown` intercepts `Tab` unconditionally) and needed no change. Discoverability is addressed with `aria-keyshortcuts="ArrowRight"` on the row plus one
shared `Typography variant="caption"` hint as the `Menu`'s last child, rendered only when at least one entry renders, carrying no `tabindex` so MUI's `activeItemIndex` lookahead never makes it a focus stop. All of this is proven in a real
browser (`tests/system/tests/search.spec.ts`, real JVM backend, keyboard-only, two deterministic entries) with a negative-control probe (the `ArrowRight` binding disabled, the new test observed to fail, the file restored and confirmed
byte-identical by SHA-256 before the passing re-run) and additive static-contract coverage in `RecentSearches.test.tsx`. `F-SEARCH-RECENT`'s `gaps` entry is narrowed rather than deleted: keyboard/assistive-technology *reachability* is now
evidenced, but a real-screen-reader (NVDA/JAWS/VoiceOver) verification of the announcement and hint discoverability remains unperformed by this project's harness, and `backlog` says so plainly with no task yet owning it. `F-SEARCH-RECENT`
stays `visual.status: proposed`; the hint joins the same outstanding acceptance FM-038 left open, additively, with one new `keyboard-refill-hint` contract state and its capture. No `data-testid` is removed or renamed, and
`SearchPage.tsx`/`SearchPage.test.tsx` are untouched. Only the coordinator marks it `done`, after the *Fresh Review* this packet requires.

## Blocked

None.

## Upcoming

- FM-022: Download History Route

**FM-048 is `done`** — reviewed **PASS** with no findings. It is a registry-contract reconciliation packet for two consequences ADR-0011 placed on records FM-042 was not allowed to touch, and it changes no React source, no test, and no
user-observable behavior: `C-RESULT-TABLE`'s `responsibility` gains the fluid, never-horizontally-scrolling layout model ADR-0011's `Consequences` says it should record when next reconciled — load-bearing rather than cosmetic, because
the absence of a scrolling ancestor between a sticky `<th>` and the document is exactly what makes the viewport-sticky column header work — and `F-SEARCH-RESULTS`'s visual contract records `desktop-wide` 1900x1000 in its structured
`contract.viewports` array instead of in free text, so `npm run validate:migration` machine-checks it. Both items were attempted as `/fm-quickfix` fixes and correctly refused by that command's gate, which forbids any registry-contract
edit; both sat under `MAINTENANCE.md`'s *Open candidates* already routed here, and FM-048 removes them from that list. The viewport stays **scoped to FM-042's own scrolled/title-collapse states**, as ADR-0011's `Human Decision` item 3
decided: the ~28 existing "at desktop" checks in `F-SEARCH-RESULTS` and `F-SEARCH-SORT-FILTER` keep their phrasing, `F-SEARCH-SORT-FILTER`'s own `contract.viewports` is confirmed unchanged rather than edited, and widening either is an
escalation to the repository owner, not a choice available to the implementer. No visual acceptance is created, implied, or re-dated: `F-SEARCH-RESULTS` and `F-SEARCH-SORT-FILTER` stay `visual.status: proposed` with human acceptance
outstanding, and all four `proposed` variances — the two ADR-0011 ones included — stay `proposed`. `validate:migration` is the load-bearing gate and the packet requires proving it actually fails on a mistyped viewport name before
accepting that it passes; the React quality chain and every Playwright run are explicitly not required and must be recorded as not run, since no file outside `docs/frontend-migration/` changes. Reviewed **PASS** with no findings by a fresh independent reviewer, which reproduced the negative probe first-hand rather than trusting the handoff: it mistyped the array entry to `desktop-wideX`, observed `validate:migration` fail with exit 1 and the exact message `FEATURES.yaml F-SEARCH-RESULTS visual contract requires named integer viewports`, reverted, diffed against a pre-probe backup to prove byte-identity, and observed it pass again — leaving no residue in the tree. It also reproduced the handoff's SHA-256 manifest for all four files, confirmed `core/ui-react/`, `tests/system/`, `core/ui-src/`, `decisions/`, and `APIS.yaml` byte-identical to baseline, read both edited records in full to confirm `task: FM-012` and every other `C-RESULT-TABLE` field untouched and `F-SEARCH-SORT-FILTER` unchanged, and spot-checked the three `results.spec.ts` `desktop-wide` call sites (`:1978`, `:2342`, `:2475`) that justify the not-run claims.

**FM-042 is `done`.** `ADR-0011` (`decisions/ADR-0011-results-table-scroll-model-and-sticky-header.md`) is **accepted** (2026-08-18, explicit decision of the repository owner): **Option E** -- the results table never scrolls horizontally, as the legacy AngularJS view does not, so the `overflowX: "auto"` wrapper and the `TABLE_MIN_WIDTH` floor are removed and native `position: sticky` column headers now work at every width at and above the stacking breakpoint -- with sub-decision **E-title (i) wrap** (`overflow-wrap: anywhere` on the title cell, variable row heights accepted) and a `desktop-wide 1900x1000` evidence viewport scoped to FM-042's own visual states. The toolbar half (verified pinned in a real browser by a previous invocation) and the header half (completed by this one) are both done: the `<colgroup>` is re-proportioned (`34%/11%/11%/9%/11%/9%/15%` for Title/Indexer/Category/Size/Details/Age/Actions), measured in a real browser at 1280x800 in both sidebar states and at 1900x1000 rather than implemented from ADR-0011's unmeasured sketch; the stacked-card/drawer breakpoint moves from MUI's `sm` to a shared raw 768px query; the sticky header's bottom edge is a `box-shadow` on each `<th>`, verified to render under `border-collapse: collapse` in this Chromium build without disturbing FM-041's inset recency stripe. Full verification passed: `core/ui-react`'s typecheck/lint/format/226-test vitest/build/check:api/validate:migration, `tests/system`'s `tsc --noEmit`, and a real-backend Playwright run of `results.spec.ts` (22/22) and `search.spec.ts` (14/14). The acceptance is not visual acceptance: `F-SEARCH-RESULTS` and `F-SEARCH-SORT-FILTER` stay `visual.status: proposed`, and the two new `proposed` ADR-0006 variances (divergence from the mock's dual-axis scroll model, and wrapping rather than ellipsizing titles) each still need explicit human acceptance. See the task packet's Handoff for the seven ADR-0011 re-measurement numbers and the full verification record. Reviewed **PASS** with no findings by a fresh independent reviewer, which reproduced every cheap deterministic command first-hand (226/226 vitest across 38 files, lint/format/typecheck/build/check:api/validate:migration, `tests/system` `tsc --noEmit`, `git diff --check`), confirmed the handoff's SHA-256 manifest still matches the on-disk files, and opened all five evidence captures directly rather than trusting their filenames. It reused the expensive real-backend Playwright run under its evidence-reuse exception, justified by the matching hashes, the passing deterministic suite, and a full read of the new spec code. It verified mechanically that no `data-testid` was removed or renamed, that `theme.ts`, `COMPONENTS.yaml`, and `validate-migration.mjs` are byte-identical to baseline, and that `results.spec.ts`'s diff is purely additive with no existing assertion altered. One non-blocking observation is carried rather than actioned: because no intermediate commit exists between this task's invocations, the claim that the toolbar-measurement machinery was preserved unrewritten cannot be confirmed byte-for-byte -- a process-transparency limit of reviewing uncommitted multi-invocation tasks, not a defect in this implementation. All three disclosed out-of-scope follow-ups have since been discharged and none remains open: `validate-migration.mjs`'s hardcoded `desktop`/`mobile` viewport allowlist by quickfix `b1bf2770a` and the resulting structured `contract.viewports` entry by FM-048; the pre-existing Size-column raw-byte-count display gap by quickfix `066db3089`, which mirrors legacy's `byteFmt` rather than inventing a format; and `COMPONENTS.yaml`'s `C-RESULT-TABLE` fluid-table note by FM-048, after `/fm-quickfix` correctly refused it as a registry-contract edit.

**FM-047 is `done`**, closed without an independent reviewer pass by explicit human decision on proportionality grounds (the deviation and what it cost are recorded in the packet) — a deliberately minimal corrective packet, not a feature, and it repairs the one long-standing unowned test defect that FM-038 introduced and that FM-038's own review, FM-044, and FM-041 each recorded and
correctly declined to fix as out of scope. `tests/system/tests/search.spec.ts` asserted the recent-search Refill control as a `menuitem`, but FM-038 made it an `IconButton` nested inside the `Repeat:` `menuitem`, so its role is
`button`; both locators now read `getByRole("button", {name: /^Refill:/})`, matching the accessible name the component actually renders and the form `RecentSearches.test.tsx:62` already used. The diff is exactly two lines. Verified
first-hand rather than inherited: Playwright's `name` is a case-insensitive substring match by default (`types.d.ts:3149-3153` — the packet's cited `:3906`/`:8115` document `hasNotText` instead, recorded as a deviation) and its
`queryRole` matches roles per element with no accessibility-tree pruning, so the nested button is discoverable. **The full 14-test `search.spec.ts` file passes against a real Maven-built JVM backend, mockserver, and the sonarr/radarr
Docker fixtures — `14 passed (26.4s)`, exit 0, no `--grep` narrowing** — and with it the `Repeat` assertion at `:598`, which no run had ever reached because the Refill failure aborted the test first, was observed executing and
passing. The React implementation is correct and was left untouched, `FEATURES.yaml` is unchanged (independently confirmed: `F-SEARCH-RECENT`'s `selectors` was already empty *before* FM-038, so it is not stale for this defect's
reason, and its `tests` and `visual` note are current), and no visual acceptance is created or implied — `F-SEARCH-RECENT` keeps its inherited `proposed` status with human acceptance still pending. FM-047 has no feature dependency on
FM-042 and shares no file with it. One accessibility concern is recorded in the packet as a follow-up proposal rather than acted on: a focusable `IconButton` inside a `MenuItem` is likely not reachable by MUI's arrow-key menu
navigation, so Refill may be keyboard-inaccessible — but FM-038's single-row structure was an explicit human instruction, and changing it is a UX decision, not a test repair.

**FM-041 is `done`** — reviewed `PASS WITH MINOR FINDINGS` with no required corrections. Every display preference for the results list now lives in one `display-options` popover on the mock's `#2a3133`/`11px`/`220px` surface, opened by a right-aligned `display-options-toggle` in
`results-selection-actions`: the two grouping toggles relocated out of the inline toolbar row with unchanged labels, defaults, and behavior; new opt-in `Compact rows` and `Highlight recent` preferences persisted alongside sorting,
filters, and `sidebarCollapsed` in the existing `hydra.search-results.table` payload — **two** new keys, since the sidebar shortcut adds none; and a "Show refine sidebar" entry that reads and writes whichever per-viewport refine-surface
mechanism is mounted. FM-045's below-`sm` `drawerOpen` was lifted verbatim into `SearchResults.tsx` as a controlled prop pair (still initialized closed, still unpersisted, its in-file rationale intact) and both files resolve the branch
from one exported `useCompactRefineSurface` hook, so they cannot drift. The two mechanisms are deliberately **not** merged and no mount-time guard was added; that divergence from the mock's single `showFilters` boolean is recorded as
one `proposed` variance on `F-SEARCH-SORT-FILTER`. All 18 `results.spec.ts` tests pass against the real backend (17 pre-existing plus the new evidence block), and FM-045's clipped, acceptance-pending `refine-sidebar-mobile-drawer` block
and capture are untouched. `F-SEARCH-RESULTS`, `F-SEARCH-SORT-FILTER`, and `F-SEARCH-GROUP-SELECTION` all stay `visual.status: proposed` with **human visual acceptance outstanding**. Three things a reviewer should read rather than
assume. The compact row padding is `4px`, not the mock's literal `7px`: FM-045 already put non-compact rows at `6px`, so the mock's value would have *increased* padding and contradicted this packet's own "default row density unchanged"
and "compact measurably shorter" criteria; compact keeps the mock's 7:11 proportion against the denser React baseline, both mock literals stay recorded in `displayStyles.ts`, and the reviewer's one actionable minor finding — that the packet's
Acceptance text still said `11px`/`7px` — was discharged before completion by a task-designer text-truth correction to `6px`/`4px` that keeps the mock literals and the 7:11 derivation visible. The packet's own
Follow-Up Work bullet still proposes that correction; it is implementer-authored handoff evidence and was deliberately left as written rather than rewritten after the fact. `Highlight recent` defaults **off** even though the mock defaults it on, because changing default rendering would invalidate accepted default-state baselines only a human may re-accept. And
below `sm` the popover cannot be opened while the modal refine `Drawer` is up (its backdrop legitimately blocks the page, as it already does for FM-045's own trigger), so the entry closes the popover when used and its closing direction
is evidenced at desktop in the browser and at mobile by the component test. `npm run format:check` fails, identically on a clean baseline tree at `6507a5ed6` — `README.md`, `SearchPage.tsx`, `router.tsx`, `tsconfig.json`,
`vite/devBackend*.ts`, and five git-ignored `.playwright-cli` artifacts, none in FM-041's write scope; the unowned pre-existing `search.spec.ts` FM-038 `Refill` failure also remains.

**ADR-0009 full-mock-fidelity batch.** FM-043 (shell theme, typography, density foundation) is `done`, so `createHydraTheme()`'s `oklch` palette, self-hosted IBM Plex Sans/Mono typography, and the denser default component sizing are
now the tokens every later packet in this batch restyles against rather than guessing at values. FM-044 (search form restyle) and FM-045 (single refine-sidebar filter surface) are no longer blocked and are independent of each other;
FM-046 remediates FM-039/FM-040 behind FM-045 and in turn unblocks FM-041. FM-046 is `done` — reviewed `PASS WITH MINOR FINDINGS` with no required corrections. The `results-toolbar` is now a flat `Box` at the mock's `16px 0 14px`
padding instead of an elevated `Paper`; `search-results-summary` gained an additive `· N selected` fragment in `primary.main` alongside the untouched `results-selected-count`; the tri-state select-all checkbox renders as the mock's
17x17px/5px-radius square through MUI `Checkbox`'s own `icon`/`checkedIcon`/`indeterminateIcon` props (ADR-0002, no bespoke control); the caret menu opens on the mock's `#2a3133`/9px popover; and "Send to downloader", the NZB ZIP
action, and every `results-download-actions` control carry the mock's palette/density with real `disabled` semantics rather than opacity. FM-040's selection/download structure and interaction logic are reused unchanged — no
`data-testid` was removed or renamed and no accessibility affordance regressed, confirmed by rerunning FM-040's existing component coverage rather than assuming it. Verification was independently reproduced by the reviewer (SHA-256
manifest matched byte for byte; `typecheck`/`lint`/`format:check`/`test`/`check:api`/`validate:migration`, `tests/system` `tsc --noEmit`, and `git diff --check` all rerun identically); the expensive real-backend Playwright run
(21/21 passing across `results.spec.ts` and `downloads.spec.ts`) was reused under the reviewer's evidence-reuse exception after the three evidence captures were opened and visually inspected. `F-SEARCH-GROUP-SELECTION`,
`F-SEARCH-DOWNLOADS`, `F-SEARCH-SAVED`, and `F-SEARCH-RESULTS` all stay `visual.status: proposed` with **human visual acceptance outstanding**. Two items are carried as accepted deviations rather than corrections. The mock's own
`allBoxBg`/`allBoxMark` logic leaves the select-all control transparent in the *indeterminate* state with only a dark `#0e1c1b` dash — which would be effectively invisible against the dark toolbar, an authoring inconsistency in the
mock; the implementation fills it like the checked state instead, preserving state visibility per ADR-0006, and that deliberate departure from the mock's literal value should be added to the packet's `Assumptions` on its next touch.
And the handoff describes the non-registry `toolbar-mock-density-desktop-bulk-actions.png` capture as documenting enabled/disabled button contrast when the single frame shows only the disabled state; the registry-cited captures are
unaffected. FM-045 is `done` — reviewed
`PASS WITH MINOR FINDINGS` with no required corrections. The `refine-sidebar` is the single result-filter surface at every viewport (FM-034's inline column-header filter popovers and the
mobile-only `results-filters`/`results-quick-filters` toolbar rows are removed; below `sm` the same toggle opens the sidebar as a MUI `Drawer`), Category/Indexer render as the mock's toggle rows, and the whole panel carries the mock's
palette/density. The no-lost-capability guarantee is structural rather than asserted: `RefineSidebar` builds one `sections` node and renders it in both the docked and `Drawer` branches, so the two viewports cannot drift apart, and both
the component and Playwright layers drive filters at 390x844 and assert the resulting row counts. It also resolves the three long-standing `results.spec.ts` failures FM-039 and FM-040 both recorded as inherited debt — all 16
`results.spec.ts` tests now pass — and leaves `F-SEARCH-SORT-FILTER` and `F-SEARCH-RESULTS` `visual.status: proposed` with **human visual acceptance outstanding**. Two FM-045 items are carried as accepted deviations rather than
corrections. Its `visual-evidence/F-SEARCH-SORT-FILTER/refine-sidebar-mobile-drawer.png` capture is **clipped** — the sidebar is taller than the 844px viewport inside the scrollable drawer, so the image bleeds underlying page content
and omits the Type chip section that the test itself asserts visible; the desktop capture is clean. Whoever performs the ADR-0006 acceptance for `refine-sidebar-mobile-drawer` should recapture it first rather than judge that state on
the current image. And `F-SEARCH-SORT-FILTER`'s `selectors` deliberately retains `freetext-filter-*` and `filter-toggle-*`, which FM-045 removed from the React target but which remain live in the legacy AngularJS view the same record
documents through its `legacy_sources` (`core/ui-src/html/states/search-results.html`, `columnFilterFreetext.html`, still exercised by passing legacy-shell tests); a task-designer refinement corrected FM-045's Acceptance to state that
rule, since deleting those entries would have made the record factually false about the legacy side. `header-filter-*` and `number-filter-*-header-*` were React-only and were deleted. FM-044 is `done` — reviewed
`PASS WITH MINOR FINDINGS`, with its one required finding a packet-internal inconsistency (its `search.spec.ts` allowlist forbade the
only conforming realization of its own collapsed-by-default Acceptance) resolved by a task-designer refinement to that packet, not by changing the implementation. FM-044 leaves `F-SEARCH-FORM`, `F-SEARCH-MEDIA`, and `F-SEARCH-INDEXERS` all `visual.status: proposed` with **human visual acceptance outstanding**, and reports one pre-existing, unrelated `tests/system/tests/search.spec.ts`
failure it did not cause and is not allowed to repair: FM-038 turned the recent-search "Refill" control into an icon `button` nested inside the `Repeat:` `menuitem` but left that spec's `getByRole("menuitem", {name: "Refill"})`
assertions untouched. Reproduced on a clean baseline tree at `68e4e2f9a`; see the FM-044 handoff's Follow-Up Work for the proposed corrective packet. Two things that landed with FM-043 are worth carrying
forward. **ADR-0010 (React Production CSS Delivery)** is accepted and implemented: the
emitted CSS entry is pinned to `assets/index.css`, `core/src/main/resources/templates/react.html` `<link>`s it render-blocking from `<head>`, and `core/ui-react/scripts/validate-production-assets.mjs` now validates that real Thymeleaf
template instead of the Vite output's unused `index.html` — so any future task importing CSS is covered by a gate that was proven to actually fail. Separately, `F-PLATFORM-SHELL`'s visual record stays `proposed`: **human visual
acceptance of the new look is still outstanding**, is independent of technical review per ADR-0006, and was not supplied by any agent.

FM-039 and FM-040 (both done) built the ADR-0008 Option B structural-redesign batch for the search page (Refine sidebar; selection-gated bulk actions bar). Per an explicit 2026-08-17 human decision, ADR-0008 is being superseded: the search
page should follow the source mock closely, including its palette and typography, not just its structure. FM-041 (display options menu) and FM-042 (sticky toolbar/header) were not started and are intentionally not promoted here; they are
being folded into the superseding initiative's task design rather than implemented against the now-superseded Option B spec. The palette and density tokens landed with FM-043, and FM-039/FM-040's remediation pass, FM-046, is now
`done`; FM-041 was wired behind it, is unblocked, and was promoted to `ready` and refined in place against ADR-0009 rather than its original Option B spec. Every ADR it names is accepted or explicitly historical. A fresh implementer
briefly marked it `blocked` on a genuine internal contradiction in its own Acceptance — "the sidebar's own toggle, and FM-045's mobile drawer trigger ... all three drive one state" is not reachable from FM-041's file scope, because
FM-045 owns the below-`sm` drawer as a deliberately local, unpersisted `drawerOpen` and its compact branch reads neither `collapsed` nor `onToggleCollapsed`. A task-designer refinement resolved it in the packet (not by changing any
implementation, and no implementation file was ever touched): `RefineSidebar.tsx`/`RefineSidebar.test.tsx` are added to the allowlist for a **mechanical lift only** of that state into `SearchResults.tsx`, and Acceptance now says
plainly that the refine surface has one live mechanism per viewport branch — the persisted `sidebarCollapsed` at `sm` and up, the unpersisted `drawerOpen` below `sm` — which the menu entry reads and writes for whichever branch is
mounted. Merging the two, and any mount-time guard forcing one from the other, is explicitly out of scope: nothing in ADR-0009, the mock (which has one boolean because it has no responsive branch), or any registry record requires it,
FM-045's in-file rationale argues against it, and persisting overlay openness would be a product decision needing an ADR. The two-mechanism realization is instead recorded as a `proposed` variance on `F-SEARCH-SORT-FILTER`. FM-045's
`refine-sidebar-mobile-drawer` spec block and its clipped, acceptance-pending capture are out of FM-041's scope and stay untouched. FM-041 is `done`. Its review reproduced all nine SHA-256 manifest entries byte for byte and independently reran `typecheck`, `lint`, `test` (38 files/223 tests), `build`, `check:api`, `validate:migration`, `tests/system` `tsc --noEmit`, and `git diff --check`; it confirmed the `format:check` failure reproduces identically on a clean baseline and touches no FM-041 path, diffed the `data-testid` sets mechanically to prove none was removed or renamed, and opened both evidence captures rather than trusting their filenames. The expensive real-backend Playwright run was reused under the reviewer's evidence-reuse exception. Two review observations are carried rather than corrected. The reviewer could not reach the mock at all — it sat at a `/tmp` path that had been cleared — so `displayMenuCaptionColor`, the popover entry metrics, the divider alpha, and the menu shadow are **not independently verified** against it and rest on the implementation's own citations. The mock has since been relocated into the repository at `uimock/NZBHydra Search.dc.html` (git-ignored, so it is present for reading but not tracked), which is the path every packet and ADR now cites; those four values can be checked directly against it whenever `F-SEARCH-RESULTS` is taken up for human visual acceptance. And `compact-rows-desktop.png` is clipped past 1280px because the table's minimum width is `1320px`, which limits what a human acceptor can judge from that frame. Compact mode also shrinks row-action touch targets, which no acceptance criterion covers — worth a future accessibility pass.

Completed work is recorded in its task packet and Git history, not listed here.
