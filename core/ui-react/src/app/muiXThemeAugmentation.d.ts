/// <reference types="@mui/x-date-pickers/themeAugmentation" />

// Declares the MuiPickers* keys of the theme's `components` map, which
// `themeComponents.ts` styles. A triple-slash reference rather than
// `import type {} from ...` because IDE import optimizers strip that import
// as unused, which has broken the typecheck twice.
